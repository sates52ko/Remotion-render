/**
 * Audio Library — Reusable audio visualization components
 *
 * Packages used:
 *   @remotion/media-utils — useWindowedAudioData, visualizeAudio, visualizeAudioWaveform, createSmoothSvgPath
 *   @remotion/noise       — noise2D (organic motion on bars)
 *   @remotion/captions    — Caption type for AudiogramCaption
 *
 * Quick import:
 *   import { AudioWaveform, MusicVisualizer, AudiogramCaption } from '../components/audio';
 */

export { AudioWaveform } from './AudioWaveform';
export { MusicVisualizer } from './MusicVisualizer';
export { AudiogramCaption } from './AudiogramCaption';
