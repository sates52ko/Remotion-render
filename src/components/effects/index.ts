/**
 * Effects Library — Animation & visual effect components for Remotion
 *
 * Import:
 *   import { AnimatedText, BubblePopText, PoppingText, FloatingChip,
 *            ParticleExplosion, CircularProgress, GlitchText, PulsingText,
 *            LiquidWave, ParallaxPan, PixelTransition } from '../components/effects';
 *
 * ── TEXT EFFECTS ────────────────────────────────────────────────────────────
 * AnimatedText   — fade / slide-up / slide-left / scale / typewriter entrance
 * BubblePopText  — characters pop in one-by-one like bubbles (with glow)
 * PoppingText    — words spring-pop sequentially (bounce / flip / zoom)
 * PulsingText    — text breathes with scale + glow pulse animation
 * GlitchText     — cyberpunk RGB-shift glitch distortion (thriller/sci-fi)
 * FloatingChip   — words appear as floating pill/badge chips
 *
 * ── VISUAL EFFECTS ──────────────────────────────────────────────────────────
 * ParticleExplosion — confetti/sparks/stars burst (SVG, physics-based)
 * CircularProgress  — animated progress ring / rating indicator
 * PixelTransition   — pixelated dissolve scene transition (in/out)
 *
 * ── BACKGROUNDS & OVERLAYS ──────────────────────────────────────────────────
 * LiquidWave   — multi-layer animated SVG wave background
 * ParallaxPan  — Ken Burns zoom/pan effect for images (6 modes)
 */

export { AnimatedText } from './AnimatedText';
export { BubblePopText } from './BubblePopText';
export { PoppingText } from './PoppingText';
export { FloatingChip } from './FloatingChip';
export { ParticleExplosion } from './ParticleExplosion';
export { CircularProgress } from './CircularProgress';
export { GlitchText } from './GlitchText';
export { PulsingText } from './PulsingText';
export { LiquidWave } from './LiquidWave';
export { ParallaxPan } from './ParallaxPan';
export { PixelTransition } from './PixelTransition';
export { TypewriterQuote } from './TypewriterQuote';
export type { TypewriterQuoteData } from './TypewriterQuote';
